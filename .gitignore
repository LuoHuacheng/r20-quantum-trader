# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environments
.venv/
venv/
ENV/

# Sensitive & Secret Credentials (CRITICAL)
.env
.env.local
# file_locks.file_lock() 的锁文件：.env 的锁名是 ..env.lock（仓根会出现，属运行时产物）
..env.lock
.env.lock
.okx/
.agents/
agent/
.claude/
.qwenpaw/
.mcp
skill.json
skills-lock.json
.skill.json.lock
agent.json
.bootstrap_completed
AGENTS.md
SOUL.md
PROFILE.md
MEMORY.md
HEARTBEAT.md
.bypy/
*.pem
*.key
*.secret
id_rsa
id_rsa.pub
token.json
credentials.json
.r20-admin-bootstrap

# Live Real-Money Ledger & Private DBs (CRITICAL)
data/*.json
data/*.db
data/*.db-wal
data/*.db-shm
data/*.sqlite
data/*.xlsx
data/*.csv
data/*.lock
data/*.tmp
# ⚠️ 原子写的**mkstemp 孤儿**：`tempfile.mkstemp(dir=DATA_DIR)` 默认名是 `tmpXXXXXXXX`
# （无后缀），上面那条 `data/*.tmp` **匹配不到** —— 实测一小时内现场留下 51 个
# 孤儿（council_config 的写临时件，共 513 KB），`git add -A` 会当新文件收进提交。
data/tmp*
data/*.bak
data/*.enc
data/*.txt
data/*.md
data/.*secret_key
data/credentials/
data/policy_archives/
trading.html
OPEN_SOURCE_CONTROL_TODO.md
CONTROL_PLANE_V2_TODO.md
!data/.gitkeep

# Live Logs & Backups & PIDs
logs/
backups/
*.log
*.tar.gz
*.zip
*.pid
*_TODO.md
R20_V600_PREVIEW_TODO.md
.github_device_pending.json

# OS & Editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Browser screenshots and temp artifacts
browser_screenshot_*.png
dashboard_*.png
linux_do_*.png
baidu_auth_*.png
admin_*.png
refined_*.png
tab_*.png
current_*.png
!docs/images/*.png
fix_*.py
ui_patch.py
refine_*.py
light_theme_*.py
premium_ui.py
chats.json
jobs.json
credentials.yaml
history.db*
checkpoints/
digest/
drivers/
media/
mem_agent/
mem_metadata/
RELEASE_NOTES_*.md
mem_session/
memory/
sessions/
skills/
tool_results/
linux_do_post_draft.html
linux_do_promo_card*.html
*_TODO.md
state/

# 推广文案与历史归档：仅本地保留，永不入库
promo_local/
.archive/
plan_local/

# 网关运行态数据库（本地运行产物，永不入库）
data/r20_gateway.sqlite3*
data/*.sqlite3

# 崩溃转储：本机 core_pattern=core + ulimit -c unlimited ⇒ 任何崩溃都会在仓库根丢一个
# ~190M 的 core 文件，曾被我一次 git add -A 误提交。按后缀一概忽略。
# ⚠️ 第一百五十五刀修正：原先写成裸 `core`（未锚定）⇒ git 的"路径组件"匹配会连带忽略
# **任何**名为 core 的目录，包括 `tests/core/` —— 于是本会话新增的 5 个门文件
# （live_artifact_shape / day_key / numeric_coercion / timestamp_unit / atomic_write_invariant）
# 一直**没被提交**，而本地全量测试照常通过（测试跑的是工作树，不是提交树）。
# 现锚定到仓库根，且只匹配崩溃转储的两种形态：
/core
/core.[0-9]*

# 观测栈的令牌文件（内容 = R20_ADMIN_TOKEN）：绝不入库，只走文件挂载
deploy/observability/metrics_token
